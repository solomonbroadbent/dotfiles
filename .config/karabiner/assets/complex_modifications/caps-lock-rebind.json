{
  "title": "caps_lock -> escape [if tapped] | right_command [if held]",
  "rules": [{
    "manipulators": [{
      "description": "caps_lock -> escape [if tapped] | right_command [if held]",
      "from": { "key_code": "caps_lock" },
      "to": [{ "key_code": "right_command" }],
      "to_if_alone": [{ "key_code": "escape" }],
      "type": "basic"
    }]
  }]
}
